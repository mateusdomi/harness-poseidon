# Threat model histórico — substituído

Status: `superseded`. Este caminho não contém regras normativas e nunca deve ser
carregado como contexto ativo.

O threat model canônico é
[`docs/backend/security/THREAT_MODEL.md`](../backend/security/THREAT_MODEL.md),
conforme a ADR-015. A entrada correspondente no manifest registra a relação de
supersession e impede duas fontes canônicas para o mesmo tópico e escopo.
