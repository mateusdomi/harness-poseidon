# Dependencies
node_modules/
# Build outputs
dist/
build/
*.tsbuildinfo
# Test artifacts
coverage/
playwright-report/
test-results/
# Storybook
storybook-static/
# Env / secrets
.env
.env.local
.env.*.local
# OS / editors
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
# Logs
*.log
.artifacts/
# .NET build artifacts (backend)
[Bb]in/
[Oo]bj/

# Frontend build output embarcado no Host (gerado por tools/backend/build-frontend.sh)
src/Harness.Host/wwwroot/

# Worktrees efêmeros de agentes da fleet (nunca versionar)
.claude/worktrees/

# SDK .NET e caches hermeticos baixados por tools/backend/install-dotnet.sh (~1 GB)
tools/backend/.tooling/
# Tambem na forma de SYMLINK: worktree nova aponta o SDK vendorizado para um clone
# existente, e a regra com barra no fim NAO pega symlink (foi como um link com
# caminho absoluto de uma maquina entrou no repositorio por um `git add -A`).
tools/backend/.tooling

# Instrumentos de QA: dependências resolvidas por symlink e saídas voláteis
artifacts/qa-poseidon/tools/node_modules
artifacts/qa-poseidon/evidencias/*.tmp
coordination/final-operation/integrator-last-run.log
coordination/final-operation/LEASE.json
coordination/final-operation/supervisor.log
coordination/final-operation/INTEGRATOR.json
.tooling/
.claude/nightshift/

# Ambiente virtual Python usado para preparação de assets da Bruna
.venv-bruna/
