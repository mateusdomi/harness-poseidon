/**
 * @deprecated Movido para `@/features/shared/components/markdown-content`
 * (reuso chat + documentos — D-027). Mantido como re-export para não
 * quebrar imports existentes do chat.
 */
export { MarkdownContent } from '@/features/shared/components/markdown-content';
