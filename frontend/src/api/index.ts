/**
 * Camada de API — implementada na fatia FE-0 B (contracts, client, realtime, fixtures).
 * Este placeholder existe apenas para o scaffolding compilar.
 */
export const API_MODE = import.meta.env.VITE_API_MODE ?? 'mock';
